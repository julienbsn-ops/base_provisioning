---
galaxy_info:
  author: julienbsn-ops
  description: Debian && Centos based distro's final tweaks
  company: infra
  license: license (BSD, MIT)
  min_ansible_version: 1.2
  platforms:
  - name: Debian
    versions:
    - latest
  - name: Centos
    versions:
    - latest
  galaxy_tags:
    - debian
    - centos
    - tweaks
dependencies: []
# List your role dependencies here, one per line. Be sure to remove the '[]' above,
# if you add dependencies to this list.
